!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title Exposition
    = stylesheet_link_tag    'admin/application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'admin/application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body
    %nav.admin
      %ul
        %li= link_to "Posts", admin_posts_path
        %li= link_to "Tags", admin_tags_path
      - if logged_in?
        %ul.login
          %li= link_to current_user.name, admin_posts_path
          %li= link_to t('sessions.destroy'),
            admin_logout_path, method: :delete
    - if flash.any?
      .flashes
        - user_facing_flashes.each do |key, value|
          %div{:class => "flash-#{key}"}= value
    = yield
